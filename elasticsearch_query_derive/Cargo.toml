[package]
name = "elasticsearch_query_derive"
version = "0.1.0"
edition = "2021"

[lib]
proc-macro = true

[dependencies]
syn = {version = "1.0", features = ["full"]}
quote = "1.0"

[dev-dependencies]
bigdecimal = { version = "0.1.2", features = ["serde"] }